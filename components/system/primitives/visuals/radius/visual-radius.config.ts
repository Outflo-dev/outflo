/* ==========================================================
   OUTFLO — VISUAL RADIUS PRIMITIVE
   File: components/system/primitives/visuals/radius/visual-radius.config.ts
   Scope: Corner radius dial
   ========================================================== */

import { createVisualDial } from "../visual.dial";

export const VISUAL_RADIUS = createVisualDial("--visual-radius");