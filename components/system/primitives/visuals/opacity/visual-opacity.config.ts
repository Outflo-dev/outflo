/* ==========================================================
   OUTFLO — VISUAL OPACITY PRIMITIVE
   File: components/system/primitives/visuals/opacity/visual-opacity.config.ts
   Scope: Shared opacity dial
   ========================================================== */

import { createVisualDial } from "../visual.dial";

export const VISUAL_OPACITY = createVisualDial("--visual-opacity");